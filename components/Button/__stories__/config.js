export const storybookBasePath = 'Buttons/';

export const argTypes = {
  children: {
    defaultValue: 'Children',
    control: { type: 'text' },
  },
};

export const decorators = [];

export const parameters = {
  controls: { expanded: true },
  layout: 'centered',
};
